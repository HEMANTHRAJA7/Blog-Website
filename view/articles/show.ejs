<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    <title>Blog Website</title>
    <style>
      body{
        padding-top: 55px;
            height: 100%;
            background: linear-gradient(120deg,#e9defa,#fbfcdb);
        }
        .button-click {
            font-family: inherit;
            padding: 0.6em 1.3em;
            font-weight: 700;
            font-size: 1em;
            border: 3px solid black;
            border-radius: 0.4em;
            box-shadow: 0.1em 0.1em;
            cursor: pointer;
        }

        .button-click:hover {
            transform: translate(-0.05em, -0.05em);
            box-shadow: 0.15em 0.15em;
        }

        .button-click:active {
            transform: translate(0.05em, 0.05em);
            box-shadow: 0.05em 0.05em;
        }

        .for-card{
            width: 80%; 
            border-top-right-radius: 100px;
            border-bottom-left-radius: 100px; 
            background: linear-gradient(120deg,#fbfcdb,#e9defa);
            border-style: solid;
            border-width: 3px;
            border-color: #f67777;
        }


    </style>
   
</head>
<body>


  <%- include("./header.ejs") %>


      <div class="container" style="height: 75vh;">
      <h1 class="my-4"><%= article.title %></h1>
      <div class="text-muted mb-4 " style="font-size: 1.2em;">
        <%=article.createdAt.toLocaleDateString() %>
      </div>
      <a href="/" class="btn button-click mb-4" style="background-color: rgba(128, 128, 128, 0.6)">All Articles</a>
      <a href="/articles/edit/<%= article.id %>" class="btn button-click mb-4" style="background-color: rgb(71, 224, 224)">Edit</a>
     <div style="font-size: 1.2em; margin-top: 50px;">
        <%- article.sanitizedHTML %>
     </div>
    </div>


    <%- include("./footer.ejs") %>

    
</body>
</html>